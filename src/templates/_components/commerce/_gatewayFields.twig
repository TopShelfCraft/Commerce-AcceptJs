<input type="{{ inputType ?? 'hidden' }}" name="dataValue" value="{{ dataValue ?? paymentForm.dataValue ?? null }}" />
<input type="{{ inputType ?? 'hidden' }}" name="dataDescriptor" value="{{ dataDescriptor ?? paymentForm.dataDescriptor ?? null }}" />
